---
import Layout from '../layouts/Base.astro';
import { getLangFromUrl } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const isEN = lang === 'en';

const title = isEN ? 'Call for Service | RotoRem Varna' : 'Обади се за сервиз | РотоРем Варна';
const heading = isEN ? 'Please Call To Schedule Your Repair' : 'Моля, обади се, за да запазиш час';
const phone = '+359 89 834 0982';
const phoneHref = `tel:${phone.replace(/\s+/g, '')}`;
const email = 'n.ivanov.ivanov@abv.bg';
---

<Layout title={title}>
  <section class="py-16 md:py-24 bg-gradient-to-b from-white to-blue-50">
    <div class="container max-w-3xl mx-auto text-center">
      <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">{heading}</h1>
      <p class="text-lg md:text-xl text-gray-600 mb-8">
        {isEN
          ? 'Online bookings are currently disabled. To arrange a technician visit, please call us directly so we can confirm the time that works best for you.'
          : 'Онлайн записванията са временно спрени. За да уговориш посещение на техник, моля обади се директно, за да потвърдим най-удобния за теб час.'}
      </p>

      <div class="bg-white shadow-lg rounded-2xl p-8 border border-blue-100 mb-8">
        <h2 class="text-2xl font-semibold text-gray-900 mb-4">
          {isEN ? 'Contact Options' : 'Начини за контакт'}
        </h2>
        <div class="space-y-4 text-lg text-gray-700">
          <a href={phoneHref} class="flex items-center justify-center gap-3 bg-blue-600 text-white rounded-lg py-4 font-semibold hover:bg-blue-700 transition-colors">
            <span>📞</span>
            <span>{phone}</span>
          </a>
          <a href={`mailto:${email}`} class="flex items-center justify-center gap-3 border border-blue-200 rounded-lg py-4 font-semibold text-blue-700 hover:bg-blue-50 transition-colors">
            <span>✉️</span>
            <span>{email}</span>
          </a>
        </div>
      </div>

      <div class="grid md:grid-cols-2 gap-6 text-left">
        <div class="bg-white rounded-xl shadow-md p-6 border border-gray-100">
          <h3 class="text-xl font-semibold text-gray-900 mb-3">
            {isEN ? 'What happens after the call?' : 'Какво следва след обаждането?'}
          </h3>
          <ul class="list-disc list-inside text-gray-700 space-y-2">
            <li>{isEN ? 'We confirm the address and the preferred time slot.' : 'Потвърждаваме адреса и удобния за теб часови диапазон.'}</li>
            <li>{isEN ? 'You receive a diagnostic price upfront (30 BGN).' : 'Получаваш предварителна цена за диагностика (30 лв.).'}</li>
            <li>{isEN ? 'The technician arrives prepared with the most common spare parts.' : 'Техникът идва подготвен с най-често нужните резервни части.'}</li>
          </ul>
        </div>
        <div class="bg-white rounded-xl shadow-md p-6 border border-gray-100">
          <h3 class="text-xl font-semibold text-gray-900 mb-3">
            {isEN ? 'Need written confirmation?' : 'Нужен ли ти е писмен отговор?'}
          </h3>
          <p class="text-gray-700">
            {isEN
              ? 'Send us an email with the appliance issue and your contact details. We will call you back to confirm the visit.'
              : 'Изпрати ни имейл с проблема на уреда и контакт за връзка. Ще се обадим, за да потвърдим посещението.'}
          </p>
        </div>
      </div>

      <p class="mt-10 text-gray-500 text-sm">
        {isEN
          ? 'Thank you for trusting RotoRem Varna. We strive to provide fast, reliable service that keeps your appliances running and your home comfortable.'
          : 'Благодарим, че избра РотоРем Варна. Ще се погрижим уредът ти да работи отново и домът ти да остане комфортен.'}
      </p>
    </div>
  </section>
</Layout>
